name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  web:
    name: Web GIS (build & test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run build

  qgis-plugin:
    name: QGIS plugin (pure-python tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install shapely h3 pytest
      - run: pytest qgis-plugin -q
